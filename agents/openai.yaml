interface:
  display_name: "ProGraph"
  short_description: "Plan or audit software projects"
  default_prompt: "Use $prograph to plan this project before implementation or audit an existing project for risks and next steps."

policy:
  allow_implicit_invocation: true
